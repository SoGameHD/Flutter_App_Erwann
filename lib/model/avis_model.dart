import 'package:flutter/cupertino.dart';

class Avis{
  int id;
  String title;
  String username;
  String profile;
  int note;
  String detail;

  Avis(this.id, this.title, this.username, this.profile, this.note, this.detail);
}